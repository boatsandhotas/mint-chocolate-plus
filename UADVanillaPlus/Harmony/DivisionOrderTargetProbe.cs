// DivisionOrderTargetProbe removed: the targeting-flow question it answered is resolved — the native
// "click a target division" click DOES route through Division.SetScreenDivision (self=ordered division,
// arg=clicked anchor), so the Parallel order captures the anchor by prefixing that method (see
// ParallelOrderPatch). Kept as an empty file to avoid a stale reference; safe to delete.
